import 'package:flutter/material.dart';

const kPrimaryColor = Color(0xFFFFC107);
const kSecondaryColor = Color(0xFF1A237E);
const kBackgroundColor = Color(0xFFF5F5F5);
const kWhiteColor = Color(0xFFFFFFFF);

const kRedGradient = LinearGradient(
    colors: [Color(0xFFE65100), Color(0xFFFFCDD2)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight);

const kBlueGradient = LinearGradient(
    colors: [Color(0xFF1565C0), Color(0xFFBBDEFB)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight);

const kYellowGradient = LinearGradient(
    colors: [Color(0xFFFFC107), Color(0xFFFFF9C4)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight);

const kGreenGradient = LinearGradient(
    colors: [Color(0xFF4CAF50), Color(0xFFC8E6C9)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight);

const kPurpleGradient = LinearGradient(
    colors: [Color(0xFF6A1B9A), Color(0xFFE1BEE7)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight);

const kShadow =
    BoxShadow(offset: Offset(5, 10), blurRadius: 10, color: Colors.grey);
